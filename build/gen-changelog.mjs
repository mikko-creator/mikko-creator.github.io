// Generate docs/CHANGE-LOG.md from the build's own evidence files.
import fs from 'node:fs';

const J = f => JSON.parse(fs.readFileSync(f, 'utf8'));
const rep = J('build/build-report.json');
const man = J('build/page-manifest.json');
const ver = J('build/verify-report.json');
const inj = J('build/injection-removals.json');
const gen = J('build/generated-manifest.json').images;
const trace = J('build/fabrication-traceability.json');

const L = [];
L.push('# Change log');
L.push('');
L.push('Every decision taken between the live site and this rebuild.');
L.push('Nothing was dropped without a reason written here.');
L.push('');
L.push('## Summary');
L.push('');
L.push('| | |');
L.push('|---|---|');
L.push('| source pages crawled | 178 (172 extracted) |');
L.push(`| pages rebuilt | ${rep.built} |`);
L.push(`| pages deliberately removed | ${man.removed.length} |`);
L.push(`| pages added | ${(rep.added || []).length} |`);
L.push(`| content recall (measured) | ${ver.avgRecall}% |`);
L.push(`| generated images | ${Object.keys(gen).length} |`);
L.push(`| real photos / logos carried over | ${rep.assetsCopied} |`);
L.push(`| pages with spam injection removed | ${inj.pages} |`);
L.push(`| claims traced verbatim to the source | ${trace.verbatimInSource} / ${trace.total} (${trace.percentSourced}%) |`);
L.push('');

L.push('## PRESERVE — the default');
L.push('');
L.push(`Every page not listed below was rebuilt with its copy, headings, title, meta`);
L.push(`description, canonical and internal links carried across. Measured content`);
L.push(`recall across all ${ver.pages} pages is **${ver.avgRecall}%** (\`build/verify-report.json\`).`);
L.push('The shortfall is concentrated in form-control text and repeated furniture,');
L.push('not in prose.');
L.push('');

L.push('## REMOVE — with reasons');
L.push('');
L.push('### Pages');
L.push('');
L.push('| URL | why |');
L.push('|---|---|');
for (const r of man.removed) L.push(`| \`${r.path}\` | ${r.why} |`);
L.push('');

L.push('### Content inside pages that were kept');
L.push('');
L.push('| what | where | why |');
L.push('|---|---|---|');
L.push(`| Hidden spam-link injection | ${inj.pages} pages, ${inj.totalBytes.toLocaleString()} bytes | Cloaked \`rel="dofollow"\` links to 60 gambling/adult domains. Not page copy; reproducing it would carry a live Google penalty into the new site. See FINDINGS.md §1. |`);
for (const l of rep.loremStripped || []) {
  L.push(`| Lorem Ipsum placeholder (${l.blocks} blocks) | \`${l.path}\` | ${l.why} |`);
}
L.push('| Page-builder shortcodes in meta fields | `/our-team` og:description | Raw `[et_pb_section …]` markup was being served as the social preview. |');
L.push('| `href="#"` anchors | site-wide | A link to nowhere. The label text is kept as copy; only the dead anchor is dropped. |');
L.push('| WordPress / Elementor / WooCommerce runtime | all pages | Platform removal is the point of the exercise. `sr-decontaminate` reports CLEAN. |');
L.push('');

L.push('## ADD — with reasons');
L.push('');
L.push('| what | why |');
L.push('|---|---|');
for (const a of rep.added || []) L.push(`| \`${a.path}\` | ${a.why} |`);
L.push(`| Generated imagery (${Object.keys(gen).length} images) | The source media library is ~356 images short (dead GCS bucket, FINDINGS.md §2). Generated art is abstract or environmental ONLY — it never depicts a person, a client, a screenshot or a result. Prompts and seeds are recorded in \`build/generated-manifest.json\`. |`);
L.push('| `sitemap.xml`, `robots.txt`, `404.html` | The source served no sitemap usable by a static host. |');
L.push('| JSON-LD (`ProfessionalService`, `BreadcrumbList`) | Built only from facts already on the page — phone, locality, and the rating the site itself publishes. |');
L.push('');

L.push('## IMPROVE — same content, better behaviour');
L.push('');
L.push('| change | why |');
L.push('|---|---|');
L.push('| `/seo-services/<city>-seo` now canonicals to `/our-locations/<city>-seo` | 25 pages of byte-identical duplicate content were competing with themselves. Both URLs still resolve. |');
L.push('| `prefers-reduced-motion` honoured | The source ignored it. Revealed content is forced *visible* under the preference, not merely un-animated. |');
L.push('| Result figures are not count-up animated | A count-up renders "Over 600%" as "60%" for most of its run — a false claim on a page selling a guarantee. |');
L.push('| All 50 reviews kept in the DOM | Nine are shown; the rest collapse behind a toggle, so no testimonial is deleted from the page. |');
L.push(`| Semantic landmarks, one H1 per page, skip link, alt text | 0 accessibility issues across ${ver.pages} pages (\`build/verify-report.json\`). |`);
L.push('| Nav collapses at 1180px | The desktop row measured 1353px and overflowed a 1280px viewport. |');
L.push('| Explicit width/height on images; lazy below the fold | Layout stability and load performance. |');
L.push('| Reveal has a failsafe | `IntersectionObserver` is suspended in a background tab; without the failsafe a page opened in one would stay blank. |');
L.push('');

L.push('## Revisions (round 2)');
L.push('');
L.push('| # | request | what changed |');
L.push('|---|---|---|');
L.push('| 1 | Hero pushed below the fold | `.hero` top padding was `clamp(4rem, 3rem + 9vw, 9rem)` = **144px** on top of a 97px sticky header, putting the H1 at y=309 and the CTAs at y=799 — off-screen on a 588px viewport. Padding is now ~31px, the internal rhythm is tightened, and the hero image went from a `4/5` portrait crop to `5/4` with a `max-height`. Measured after: H1 at y=99, CTAs at y=553, **in the first fold**. |');
L.push('| 2 | Remove section-title em dashes | The three em dashes authored into visible copy (closing CTA, footer, contact lede) are gone. Measured: `0` em dashes in rendered body text. Dashes that belong to SOURCE content — e.g. the client name "Raich Law – Business Lawyer Las Vegas" — are preserved, since removing those would edit the client\'s copy. |');
L.push('| 3 | More James Sutton imagery | The source published four usable photographs of the founder; the rebuild was surfacing two. The founder section now leads with the `james-approved` cut-out over a violet glow with the MyVegas cover as a proof inset, and the guarantee band gained the `james-fpsg-footer` cut-out. Homepage now carries 4 James images, all loading. |');
L.push('| 4 | `+345%` pill broken / overlapping / no animation | It sat at `left:-8%`, hanging 40px off the hero image\'s left edge, and carried no reveal. It now tucks inside the image\'s bottom-left corner (`floatInsideImage: true`), is width-capped so it cannot spill, animates in with `data-reveal="scale"` and drifts on a `float-slow` loop. It goes `static` below 640px. |');
L.push('| 5 | Text with no margin from its container | **Root cause: eight invalid `clamp()` values.** CSS requires whitespace around `+` inside `clamp()`/`calc()`; `clamp(1.75rem,1rem+3vw,3.5rem)` is invalid, so the browser dropped the entire `padding` declaration and those glass panels computed to `padding: 0`. All eight are repaired. Measured: **22 → 0** text-touching-edge violations, and 0 across 60 page×viewport combinations. |');
L.push('');
L.push('Verified after the revisions: content recall unchanged at 96.39%, decontamination CLEAN,');
L.push('and a 12-page × 5-viewport sweep (1440/1280/900/768/390) reporting 0 spacing violations,');
L.push('0 horizontal overflow and 0 broken images.');
L.push('');

L.push('## Revisions (round 3) — adversarial design review');
L.push('');
L.push('A six-lens review (spacing, typography, imagery, design-system consistency,');
L.push('responsive, accessibility) raised findings that were then adversarially verified');
L.push('against the real files. **21 confirmed, 3 refuted and dropped.** All 21 are fixed.');
L.push('');
L.push('### Accessibility');
L.push('');
L.push('| fix | measured |');
L.push('|---|---|');
L.push('| `--fg-dim` (#877f9c) retired as text on glass — `.stat__pre`, `.review__src`, `.form-note`, `.contact-line__label`, and input placeholders now use `--fg-muted` | those selectors were 3.4–4.7:1 (below the 4.5:1 floor); they now measure **8.35–9.91:1** |');
L.push('| The hero `+345%` panel sits over a photograph, where a white glass wash is not a scrim. It now has its own dark backing. | label was **1.08:1**; now **9.91:1** |');
L.push('| Heading hierarchy: `renderProse` and `leftoverSection` now track the previous level, footer column titles promoted to `h2`, and listing-card titles promoted to `h2` | **33 → 0** heading-level skips across 151 pages |');
L.push('| Form fields lost all focus indication (`outline: none`); restored via `:focus-visible` so it survives forced-colors mode | — |');
L.push('| `--step-0` floor raised to `1rem` — inputs were 15.2px, and iOS Safari zooms the viewport on any field under 16px | — |');
L.push('| The no-`backdrop-filter` fallback covered 3 of 12 glass surfaces; the other nine would have rendered as near-transparent text | — |');
L.push('');
L.push('Gold buttons and gradient text were checked separately, since a computed-style');
L.push('reader sees `color: transparent` / a gradient `background` and cannot evaluate them:');
L.push('`--ink-900` on the gold gradient measures **5.31–12.95:1**, and the gold gradient on');
L.push('the hero scrim **8.75–15.79:1**. Both pass.');
L.push('');
L.push('### Layout and system');
L.push('');
L.push('| fix | why it mattered |');
L.push('|---|---|');
L.push('| `.founder-figure__inset` mobile width override was dead | the base rule `.hero__visual .founder-figure__inset` (0,2,0) outranked the bare class in the media query (0,1,0) — a specificity bug introduced by the round-2 crop fix |');
L.push('| `.page-hero__media::after` ends at an OPAQUE `--bg-deep`; reused inside the CTA band it painted a solid block over the lower half of the glass | scoped a translucent variant to the band |');
L.push('| `.cta-band p` was unscoped and outranked every nested class, forcing lede-size type and a 2rem margin onto micro-labels | scoped to direct children |');
L.push('| `.card p { margin-bottom: 0 }` collapsed every multi-paragraph card | added `.card p + p` |');
L.push('| `.pulse` animated `box-shadow` on `.btn--gold`, cancelling the button\'s own shadow | ring moved to a pseudo-element |');
L.push('| Header CTA is hidden below 1180px and was never re-offered | a duplicate CTA now lives in the nav panel; the CTA is reachable at 390/768/1180/1440 |');
L.push('| Base `li` margin leaked into the nav; no `ul:last-child` margin reset; `.hero__visual::after` frame not reset at ≤640px | — |');
L.push('| `--step-5` was a broken rung (1.115× over `--step-4` where every other pair is 1.20–1.30×) | re-derived |');
L.push('| Founder and CTA figures shipped with no `width`/`height` | intrinsic dimensions added, so height is reserved before decode |');
L.push('| Five dead rules removed (`.glass--flush`, `.btn--ghost`, `.card__icon`, `.divider`) | — |');
L.push('| `html { overflow-x: clip }` added | the reveal translates 34px horizontally; `body` clipped it but `html` did not |');
L.push('');
L.push('Re-verified after: **65 combinations** (13 pages × 1440/1280/900/768/390) reporting');
L.push('0 spacing violations, 0 overflow, 0 broken images and 0 heading skips; content recall');
L.push('unchanged at 96.39%; decontamination CLEAN; sticky header still functional under');
L.push('`overflow-x: clip`.');
L.push('');

L.push('## Revisions (round 4)');
L.push('');
L.push('**1. "Section titles still have em dashes."** There were no dash *characters* in any');
L.push('section title — the culprit was CSS. `.eyebrow::before` painted a 28px × 1px gold rule');
L.push('immediately before every eyebrow label, which reads as an em dash: it rendered on 744');
L.push('eyebrows across 151 pages. The rule is deleted. Measured after: **689 eyebrows, 0 with');
L.push('a dash**; of 2,477 headings only 2 contain one, and both are the client\'s own data —');
L.push('the business name "Raich Law – Business Lawyer Las Vegas" and a phone/hours line');
L.push('("Mon–Fri 8:30am–5pm"). Those are left alone; editing them would rewrite client copy.');
L.push('');
L.push('**2. Reviews carousel.** 50 stacked cards made the section 1,630px tall. It is now a');
L.push('seamless infinite loop, and the "See all 50 reviews" button sits at the bottom of the');
L.push('section as before.');
L.push('');
L.push('| | |');
L.push('|---|---|');
L.push('| section height | **1,630px → 975px** |');
L.push('| reviews in the served HTML | **50** — each exactly once |');
L.push('| loop duplicate | created by `app.js` at runtime and marked `aria-hidden`, so screen readers and crawlers still see 50, not 100 |');
L.push('| long reviews | one is 963 characters and was forcing every flex card to 1,038px. In loop mode the text is line-clamped; the full string stays in the DOM and is shown in full when the section is expanded, so nothing is dropped |');
L.push('| pause | on `:hover` and on `:focus-within`, so it stops while being read or tabbed through |');
L.push('| `prefers-reduced-motion` | no auto-scroll; the row becomes a normal scrollable strip |');
L.push('| "See all reviews" | swaps the loop for a full 3-column grid with every review unclamped, and hides the duplicate set so nothing appears twice |');
L.push('');
L.push('**3. Navigation was too crowded.** SEO, WEB and CRO were three separate top-level items.');
L.push('They now live inside a **Full Suite mega-menu**, which drops the bar from **10 items to 7**.');
L.push('');
L.push('The mega-menu has three columns: the suite itself (with its real one-line promise and a');
L.push('link through), the three services that have their own pages, and the five that do not —');
L.push('Content Writing, Reputation Management, Graphic Design, Branding, Consulting. Those five');
L.push('are rendered as **text, not links**, because they have no page to point at and a link to');
L.push('nowhere is the dead-link defect the gate exists to catch. Every label is lifted from the');
L.push('source\'s own "FULL SUITE INCLUDES" section and hero pills; none is invented.');
L.push('');
L.push('Because the bar now needs ~955px instead of ~1,353px, the hamburger breakpoint moved');
L.push('from 1180px down to **980px** — tablets keep the real navigation. Verified: desktop nav');
L.push('with a 3-column mega-menu at 1000px and up, hamburger with a stacked 1-column mega-menu');
L.push('at 960px and below, the CTA reachable at every width, and 0 header overflow throughout.');
L.push('');
L.push('Re-verified after: **78 combinations** (13 pages × 1440/1280/1024/900/768/390) with 0');
L.push('spacing violations, 0 overflow, 0 broken images and 0 heading skips; content recall');
L.push('**96.46%**; 0 SEO issues; 0 accessibility issues; decontamination CLEAN.');
L.push('');

L.push('## Revisions (round 5)');
L.push('');
L.push('**Logo enlarged in the navigation bar and the footer.**');
L.push('');
L.push('| | before | after |');
L.push('|---|---|---|');
L.push('| header mark | 38px (32px in the mid-width band) | **52px at every width** |');
L.push('| footer mark | 38px (shared the header rule) | **68px** (its own rule — the footer has more room) |');
L.push('| declared `width`/`height` | `160×80` | **`250×122`** — the file\'s real size |');
L.push('');
L.push('The declared attributes were the wrong aspect ratio (2:1 against the file\'s 2.05:1),');
L.push('so the reserved box did not match what decoded and the mark shifted slightly on load.');
L.push('Corrected on all 302 logo tags.');
L.push('');
L.push('Two knock-on fixes the change forced, both measured rather than assumed:');
L.push('');
L.push('1. **The header CTA overflowed in a mid-width band.** With the larger mark the desktop');
L.push('   bar needs ~1140px of layout width; below that the "Start Here" button spilled past');
L.push('   the header (66px at 1000px, 115px at 1121px). The hamburger breakpoint moved from');
L.push('   980px to **1140px**, and the mega-menu\'s stacked form and the JS breakpoint moved');
L.push('   with it — previously they sat 40px apart, which would have left a band where the nav');
L.push('   collapsed but the mega-menu stayed absolutely positioned.');
L.push('2. **A 1px broken band at the boundary.** The mid-width tightening rule was written as');
L.push('   `(max-width: 1400px) and (min-width: 1141px)`; at exactly 1141px it failed to match,');
L.push('   so the bar rendered untightened (nav 829px instead of 702px) and the CTA overflowed');
L.push('   by 97px. The lower bound was redundant — below 1140px the hamburger takes over — so');
L.push('   it is gone and the band is closed.');
L.push('');
L.push('Verified after: the logo renders at 52/68px with the correct aspect ratio at every');
L.push('width from 390px to 1600px, 0 CTA spill and 0 document overflow across 11 widths, the');
L.push('hero CTAs still land in the first fold, and a 78-combination sweep (13 pages ×');
L.push('1440/1200/1141/1024/768/390) reports 0 spacing violations, 0 overflow, 0 broken images');
L.push('and 0 heading skips. Content recall 96.46%, decontamination CLEAN.');
L.push('');

L.push('## Revisions (round 6) — alignment and image sizing');
L.push('');
L.push('Auditing for the reported misalignment turned up two defects that were worse than');
L.push('the alignment itself.');
L.push('');
L.push('### The portfolio tiles were showing the wrong pictures');
L.push('');
L.push('Tiles paired images to projects **by document order**, so they took whatever appeared');
L.push('first in the page: a 60×60 phone glyph and a 64×64 Google avatar, stretched **6×** across');
L.push('a 369px tile. Loosening the rule to "any large image" only swapped those for a contact');
L.push('graphic and the site\'s own logo.');
L.push('');
L.push('Every project\'s real screenshot is among the ~356 assets lost with the closed GCS bucket');
L.push('(FINDINGS.md §2), so **no correct image exists** and any real-image pick is arbitrary.');
L.push('Each tile now uses purpose-generated art for its own category.');
L.push('');
L.push('Two attempts were rejected on the way, and the reason matters:');
L.push('');
L.push('* **Environmental renders** (a law library, a spa treatment room) were rejected because a');
L.push('  photorealistic interior on a tile captioned "All American Law Firm" reads as *that firm\'s');
L.push('  premises* — the documentary role generated imagery must never fill (B3).');
L.push('* **Reusing the service art** was rejected because it put two glasses toasting on the law');
L.push('  tiles and a flame on a dental one: right register, wrong meaning.');
L.push('');
L.push('The five tiles that shipped are deliberately non-representational — ordered glass columns,');
L.push('concentric arcs, an open lattice, glass petals, floating panels — carrying a category by');
L.push('shape and colour alone. `fabrication:generated-proof` findings: **0**.');
L.push('');
L.push('### /our-team rendered nobody');
L.push('');
L.push('The source page is an empty Divi shell whose body is the unexpanded shortcode');
L.push('`[get_aff_team_members]`, so it extracted 0 people and the page shipped with no team on');
L.push('it. The real roster — 9 people with their photographs — is published on the homepage, so');
L.push('it is read from there. Same site, same people, nothing invented. **0 → 9 portraits.**');
L.push('');
L.push('### Alignment and sizing');
L.push('');
L.push('| fix | measured |');
L.push('|---|---|');
L.push('| Two-column media rows were centre-aligned, so columns of unequal height read as unaligned — on `/services/suite` a 205px text block sat **101px** down the side of a 408px image. Columns now share a top edge. | topDelta **101 → 0** (and 23/29 → 0 on the homepage hero and founder rows) |');
L.push('| The case study put a 698px body column beside a 324px stats column, top-aligned, stranding **374px** of dead space under the stats. | stats centre against the body |');
L.push('| Team portraits were 100px circles cropped from 350×400 originals | **148px** |');
L.push('| The founder\'s magazine inset was 150px against a 420px portrait | **clamp(120px, 34%, 190px)** |');
L.push('| Press logos were 46px in a 1160px strip | **62px** |');
L.push('| Accordion `+`/`−` glyphs (`list-add.svg`) rendered as article content, expanding to **831px** in a 740px prose column because an SVG with no intrinsic box fills its container | icon-sized and icon-named assets are skipped in prose |');
L.push('');
L.push('The layout sweep now also fails on **any image blown up past 1.25× its intrinsic width**');
L.push('(ignoring low-opacity backdrops) and on **any two-column media row whose columns differ by');
L.push('more than 12px at the top** — both defects it previously could not see.');
L.push('');
L.push('Re-verified: **78 combinations** (13 pages × 1440/1200/1141/1024/768/390) with 0 spacing');
L.push('violations, 0 overflow, 0 broken images, 0 heading skips, **0 upscaled images and 0');
L.push('misaligned rows**. Content recall 96.46%, 0 SEO issues, 0 accessibility issues,');
L.push('decontamination CLEAN, 0 generated-proof findings.');
L.push('');

L.push('## Revisions (round 7)');
L.push('');
L.push('**The raised-finger portrait moved to About Us.** `james-approved.png` left the founder');
L.push('section and now stands beside the About Us copy, which became a two-column row for it');
L.push('(text left, founder right, with a soft violet pool so the cut-out is not floating on');
L.push('nothing). It renders 360×516 from a 504×722 original — **0.71×, no upscaling** — with its');
L.push('base level with the panel\'s (bottom delta **0**). Below 900px the row stacks and the');
L.push('figure leads.');
L.push('');
L.push('The three founder photographs now sit in three different places: the magazine cover in');
L.push('the founder section, the raised finger in About Us, the hands-up cut-out in the guarantee');
L.push('band.');
L.push('');
L.push('**The MYVEGAS cover is now the founder section\'s image**, sized to the copy beside it');
L.push('rather than tucked in as a 150px inset — **150px → 475×537px**.');
L.push('');
L.push('Two things had to be undone to get there:');
L.push('');
L.push('* `.hero__visual img` caps height at `min(58vh, 460px)`, which was clamping the cover to');
L.push('  **341px** and letterboxing it inside a 5:4 box — so it was rendering landscape when the');
L.push('  file is portrait. That cap is overridden for this image.');
L.push('* The old `founder-figure__cut` / `__inset` rules became dead once the markup changed,');
L.push('  including a breakpoint override for an element that no longer exists. Removed.');
L.push('');
L.push('**On "match the size of the text":** the cover reaches **89% of the text column height**');
L.push('(537px against 602px), not 100%. The only copy of it in the harvest is 380×430 — the');
L.push('larger `MyVegasMag-Mockups.png` is among the dead GCS assets — so filling the full height');
L.push('would mean upscaling ~1.4× and visibly softening it. It is capped at 1.25×, the limit the');
L.push('layout sweep\'s own no-upscale check allows. A sharper original would close the last 65px.');
L.push('');
L.push('Re-verified: **65 combinations** (13 pages × 1440/1200/1024/768/390) with 0 spacing');
L.push('violations, 0 overflow, 0 broken images, 0 heading skips, 0 upscaled images and 0');
L.push('misaligned rows; content recall 96.46%, 0 SEO issues, 0 accessibility issues,');
L.push('decontamination CLEAN, 0 generated-proof findings.');
L.push('');

L.push('## Revisions (round 8)');
L.push('');
L.push('**The founder in the guarantee band now meets the bottom of the card.** He was floating');
L.push('with a gap beneath him; the cause was `align-self: end` stopping at the bottom of the');
L.push('band\'s CONTENT box, which sits one padding-length above the border edge — and that');
L.push('padding measured exactly **81px**, the size of the gap.');
L.push('');
L.push('A negative bottom margin equal to that padding (tracked with the same `clamp()`, so it');
L.push('follows the padding at every width) carries him down to the border edge, where');
L.push('`.glass`\'s `overflow: hidden` crops him — the torso meets the card instead of hovering');
L.push('over it. He was also rendering at only 0.46× of a 571×462 original, so the width grew');
L.push('with room to spare.');
L.push('');
L.push('| | before | after |');
L.push('|---|---|---|');
L.push('| gap to the card\'s bottom edge | **81px** | **0–1px** |');
L.push('| figure size (1440px) | 260×210 | **420×340** |');
L.push('| upscale factor | 0.46× | **0.74×** (guard trips at 1.25×) |');
L.push('');
L.push('Checked the cut-out\'s alpha channel directly rather than assuming: the PNG has **0');
L.push('transparent rows at the bottom** — the torso runs to the final pixel row (461 of 462) —');
L.push('so a flush image box really is a flush figure, not a flush box around a floating one.');
L.push('');
L.push('Verified across 1440/1200/900/390: gap 0–1px at every width, no overlap with the');
L.push('"Start Here" button, figure inside the card\'s right edge, 0 document overflow. The');
L.push('stacked layout below 760px gets the same treatment.');
L.push('');
L.push('Re-verified: **65 combinations** (13 pages × 1440/1200/1024/768/390) with 0 spacing');
L.push('violations, 0 overflow, 0 broken images, 0 heading skips, 0 upscaled images and 0');
L.push('misaligned rows; content recall 96.46%, 0 SEO issues, 0 accessibility issues,');
L.push('decontamination CLEAN.');
L.push('');

L.push('## Revisions (round 9) — mobile');
L.push('');
L.push('The desktop layout had been driving the phone. Type, padding and tap targets all');
L.push('came from fluid `clamp()` values whose lower bounds were tuned for a 1440px page, so');
L.push('at 375px they bottomed out: body copy set at **12.9px**, the smallest labels at');
L.push('**10.9px**, and section padding staying at **56px** top and bottom on all 15 sections');
L.push('— more than a seventh of a phone screen spent on empty band, twice per section.');
L.push('');
L.push('The fix is one scoped `@media (max-width: 640px)` block that re-floors the type scale');
L.push('and the spacing tokens. It changes token values, not call sites, so every component');
L.push('inherits the correction (BRAND-SYSTEM.md §tokens).');
L.push('');
L.push('### Measured, 375px viewport');
L.push('');
L.push('| | before | after |');
L.push('|---|---|---|');
L.push('| body copy | 12.9px | **16.1px** |');
L.push('| nav and footer links | 12.9px | **14px** |');
L.push('| smallest text anywhere on the page | 10.9px | **12.8px** |');
L.push('| `.btn--sm` label | 11.5px | **13.1px** |');
L.push('| H1 | 42.9px | **35.2px** (headline no longer outruns the 375px line) |');
L.push('| section padding | 56px | **44px** |');
L.push('| page gutter | 17px | **20px** |');
L.push('| tap targets under 44px | **16–17 per page** | **0** |');
L.push('');
L.push('Two details worth naming, because both are places a blanket rule would have done');
L.push('damage. Footer and breadcrumb links got padding to reach 44px, but a link inside a');
L.push('sentence must NOT become a 44px block — so the rule is scoped to `p > a:only-child`,');
L.push('a link that IS the whole paragraph and is therefore a CTA. And the H1 went *down*,');
L.push('not up: at 42.9px the headline was breaking mid-word on a 375px screen.');
L.push('');
L.push('### The one glass surface that cannot be glass');
L.push('');
L.push('Found by opening the nav drawer on screen rather than measuring it: the hero headline');
L.push('read straight through the open menu, competing with the nav labels.');
L.push('');
L.push('The cause is a CSS rule that looks correct in isolation. The drawer carried');
L.push('`background: rgba(11,4,20,.94)` **and** `backdrop-filter: blur(34px)` — but its parent');
L.push('`.site-header__inner` already has a `backdrop-filter`, and **an element with one becomes');
L.push('the backdrop root for everything inside it.** The drawer\'s blur could only ever sample');
L.push('within that root, never the page behind the header, so it silently did nothing. The 6%');
L.push('of the hero transmitted through a 0.94 fill therefore stayed perfectly *sharp* — and');
L.push('sharp 35px headline type at 6% is legible.');
L.push('');
L.push('The drawer is now an opaque `var(--bg-deep)` fill; the glass read is carried by its');
L.push('border and shadow instead. Confirmed by forcing the fill opaque in the live DOM before');
L.push('editing any CSS, so the diagnosis was proven rather than inferred.');
L.push('');
L.push('A sweep for the same trap found **19 more nested `backdrop-filter` elements** — all form');
L.push('inputs inside `.glass` cards. Their blur is equally inert, but the surface behind them');
L.push('is a flat panel with no detail to bleed through, so there is no legibility consequence;');
L.push('they are left alone and recorded here rather than churned.');
L.push('');
L.push('### Dangling in-page anchors — 63 of them');
L.push('');
L.push('Separately found while sweeping: **63 `href="#id"` links pointing at ids no page');
L.push('contains** — `#seog__inquiry` (58 refs), `#bookAppointment` (4) and `#main-content`');
L.push('(1). They addressed WordPress widgets — an inquiry modal and a booking embed — that');
L.push('this rebuild does not render, so every one clicked to nothing. `sr-decontaminate`');
L.push('only flags a bare `href="#"`, which is why they survived every earlier pass.');
L.push('');
L.push('The inquiry and booking anchors now resolve to `/contact-us`, which is what they were');
L.push('for; `#main-content` resolves to this build\'s `#main` landmark. Detection was added');
L.push('to `build/verify.mjs` so the class of defect cannot come back silently.');
L.push('');
L.push('### Verification');
L.push('');
L.push('**44 mobile combinations** (11 pages × 360/390/414/430) — 0 problems. **20 desktop');
L.push('combinations** (5 pages × 1440/1200/900/768) — 0 problems, with homepage body copy');
L.push('still 17px and section padding 31/27/22/20px, confirming the mobile block does not');
L.push('leak upward. Interactive state checked at 390 and 360: hamburger 44×44, drawer within');
L.push('the viewport, megamenu collapsing to one column, review carousel looping with a 260px');
L.push('card, hero CTA full-width and inside the first fold, 0 horizontal document overflow.');
L.push('');
L.push('Content recall 96.46%, 0 SEO issues, 0 accessibility issues, 0 broken assets,');
L.push('0 bare-`#` anchors, **0 dangling `#anchors`**.');
L.push('');

L.push('## Claims');
L.push('');
L.push(`\`sr-fabrication\` raised ${trace.total} findings. **${trace.verbatimInSource} (${trace.percentSourced}%) match text that appears`);
L.push('verbatim in `audit/raw/` — the untouched crawl.** They are the site\'s own copy and its');
L.push('real customer reviews, not invented material.');
L.push('');
L.push(`All ${trace.byCode['fabrication:statistic'].total} \`fabrication:statistic\` findings are one claim — **"3x more"** — and it is not a`);
L.push('statistic the rebuild authored. It sits inside a customer review ("I was paying 3x more');
L.push('doing this with multiple companies") that appears word for word in **165 source pages**,');
L.push('and the rebuild reproduces the whole sentence unchanged. A 7-character fragment would');
L.push('match any large corpus by accident, so it was cleared the stricter way: lift the');
L.push('surrounding sentence off the rebuilt page and require *that* span to be found intact in');
L.push(`the source. ${trace.clearedViaSentenceContext} findings cleared that way.`);
L.push('');
L.push(`The remaining ${trace.notVerbatim} findings are ${new Set((trace.items||[]).map(i=>i.claim)).size} distinct scanner artefacts of the same kind: a heading`);
L.push('concatenated across a newline with the body text that follows it — e.g. the page');
L.push('`<title>` "the best Las Vegas…" joined to "Continue reading". Each half is in the');
L.push('crawl (169 and 38 pages respectively); the joined span is not a sentence that appears');
L.push('anywhere, so there is nothing to source.');
L.push('');
L.push('**No testimonial, statistic, credential, client or guarantee in this rebuild was');
L.push('authored.** Evidence: `build/fabrication-traceability.json`.');
L.push('');

fs.writeFileSync('docs/CHANGE-LOG.md', L.join('\n'));
console.log('docs/CHANGE-LOG.md written —', L.length, 'lines');
